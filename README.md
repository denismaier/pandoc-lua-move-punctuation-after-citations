# pandoc-lua-move-punctuation-after-citations
A pandoc filter written in lua that moves punctuation after a following citation

# Usage

As usual,

```
pandoc inputfile.md -o outputfile.html -L move-punctuation-after-citations.lua
```